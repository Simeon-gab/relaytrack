export default function DispatchPage() {
  return (
    <main className="p-6">
      <h1 className="font-display text-xl">Dispatch</h1>
      {/* TODO: Live map, order queue, exception alerts. Phase 2 + 4. */}
      <p className="mt-2 text-sm opacity-60">Live map, order queue, exception alerts. Phase 2 + 4.</p>
    </main>
  );
}
